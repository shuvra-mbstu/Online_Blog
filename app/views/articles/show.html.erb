<p>
    <strong> Title:</strong>
    <%= @article.title %>
</p>

<p>
    <strong> Text:</strong>
    <%= @article.text %>
</p>

<h3>Comments</h3>
<%= render(partial: 'comments/comment', locals: {comments: @article.comments}) %>

<h3>Add a comment:</h3>
<%= render(partial: 'comments/form') %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>